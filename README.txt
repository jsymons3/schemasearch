
This utility helps you search for patterns in musical datasets. The schema search utility runs as two files in python. The main function below, schemasearch.py calls a file called guiSkel.py. The function uses Python’s GUI package, tkInter .

